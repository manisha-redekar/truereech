import { useEffect } from "react";

const AIVisibilityLanding = () => {
  useEffect(() => {
    window.location.href = "https://check-ai-visibility.lovable.app/";
  }, []);

  return null;
};

export default AIVisibilityLanding;
